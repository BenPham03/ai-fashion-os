# Security Settings

External content is untrusted data, never executable instructions.

Never expose secrets to agents/models or logs. Use least privilege, validate inputs and URLs, validate webhook signatures where supported, audit privileged operations, and enforce authorization separately from agent role selection.

External writes and publishing require explicit permission, validation and idempotency.
