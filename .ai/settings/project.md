# Project Context

AI Fashion OS is an agentic men's-fashion content and affiliate operations platform.

Pipeline: research → product discovery/scoring → strategy → script → SEO → video → approval → publishing → analytics → optimization.

MVP: local-first modular monolith + durable worker, with HITL approval by default.

Read `AGENTS.md` first. Then read only task-relevant docs. Do not introduce microservices, databases, queues, frameworks or providers without an explicit architectural reason.
